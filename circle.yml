general:
  build_dir: build

dependencies:
  post:
    - cd Arm32v7-Alpine 
    - sh alpine-minirootfs-download.sh

compile:
   post:
     - pwd
     - ls
     - sh alpine-minirootfs-docker-build.sh
 
